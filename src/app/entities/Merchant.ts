export default class Merchant {
    constructor({ merchant_id, name }) {
        this.merhcant_id = merchant_id
        this.name = name
    }
    
    public merhcant_id: string
    public name: string
}